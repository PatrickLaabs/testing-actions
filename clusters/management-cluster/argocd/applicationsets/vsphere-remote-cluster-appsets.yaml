---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vsphere-remote-cluster-appsets
  namespace: argocd
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          provider: vsphere
  template:
    metadata:
      name: 'vsphere-appsets-{{name}}'
    spec:
      project: infrastructure
      source:
        repoURL: 'git@github.com:eitco/argo-hub.git'
        path: clusters/workload-clusters/appsets
        targetRevision: 0.1.6
      destination:
        name: '{{name}}'
        namespace: default
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
        automated:
          prune: true
          selfHeal: true